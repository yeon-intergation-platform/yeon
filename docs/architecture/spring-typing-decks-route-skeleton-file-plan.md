# Spring typing-decks route skeleton file plan

1. backend controller/service/repository 추가
2. web spring client 추가
3. route layer에서 `typing-decks-service` 제거
4. default deck은 local helper로 분리
5. route tests + backend tests + build 검증
