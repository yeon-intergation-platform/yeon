# Spring card-decks route skeleton file plan

1. backend DTO/repository/service/controller 추가
2. web spring client 추가
3. route layer direct `card-decks-service` 제거
4. dead next service/test 제거
5. targeted tests + build + route audit
