# 카드 덱 생성 CTA 본문 이동 백로그 (2026-05-12)

## 1차 - 헤더 액션 정리 및 본문 CTA 배치

- 작업내용: `YEON 카드` 헤더에서 `+ 새 덱` 버튼을 제거하고, 카드 서비스 본문 `내 덱` 영역 안에 새 덱 생성 CTA를 배치한다.
- 논의 필요: 게스트 덱 병합 버튼은 계정 전환/복구성 액션이므로 헤더 유지 여부를 확인할 수 있다.
- 선택지: 1) 새 덱만 본문 이동 2) 모든 액션을 본문 이동 3) 현재 유지
- 추천: 1) 사용자가 지적한 생성 CTA만 본문으로 이동하고, 게스트 병합은 기존 복구성 알림 액션으로 유지
- 사용자 방향: 새 덱 버튼은 헤더가 아니라 덱 목록 본문 내부에 있어야 함
