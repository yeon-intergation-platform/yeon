# 차수46 CI/CD 분기 고정(스프링/Next 단독/동시)
- 생성일시: 2026-05-09T21:47:00+09:00
- 작업내용: 변경 경로별로 배포 파이프라인을 3분기로 분기한다.
  - 스프링 변경: `apps/backend` 경로 변경 시 backend 배포만 실행
  - Next 변경: `apps/web` 경로 변경 시 nextjs 배포만 실행
  - 병행 변경: 두 경로 변경 시 both 배포
- 논의 필요: 경로 필터 방식(디렉터리 기반 단순 규칙 vs 파일 단위 정밀 규칙).
- 선택지:
  1. 경로 기반 직관 규칙 사용
  2. 변경행위 기반 정밀 규칙 사용
- 추천: 경로 기반 규칙 유지 + 예외 오버라이드 목록 운용
- 사용자 방향: 추천 기준 진행
- 확장 산출:
  - 검토 워크플로: `.github/workflows/docker-image.yml` detect_changes 단계
  - 점검 항목: `apps/backend`, `apps/web`, `apps/race-server` 변경 조합 테스트
  - 실패 대응: 분기 미스매치 시 수동 재배포 승인 토글

