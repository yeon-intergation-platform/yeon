# 2026-05-17 카드서비스 + 익명친구 모바일 앱 출시: 웹 정합성/유지보수 백로그

## 현재 진행 상태 (2026-05-17)

- [x] 차수 1 — 기준(실행 원칙) 확정
- [x] 차수 2 — 공통 계약/도메인 정렬(중복 구현 축소)
- [x] 차수 3 — 카드서비스 모바일 화면 재작업: 웹 정합 기준 반영 _(문구/액션 라벨 정합 및 접근성 계약 테스트 완료)_
- [x] 차수 4 — 익명친구 앱 연동 정합 및 동시 발행 준비
- [x] 차수 5 — 단계별 점검/배포 체크

> 원칙: 차수는 **체크리스트 3개 이상 완료** 시 다음 차수로 이동.
> 완료 근거가 없으면 다음 차수로 진행하지 않음.

## 차수 1 — 기준(실행 원칙) 확정

### 작업내용

- `카드서비스`와 `익명친구`를 같은 코드베이스에서 운영하되, **웹 완전 복제 대신 웹 동작 규격 기반의 RN 렌더링 전략**으로 확정.
- 유지보수 영역은 `apps/mobile`, `packages/api-contract`, `packages/api-client`, `apps/web`의 공통 계약/로직으로 한정.
- 익명친구는 전화번호 인증 스킵 기본값 유지, 카드서비스는 기존 인증 흐름 유지.

### 논의 필요

- 웹 정합도 목표를 **픽셀 동일**로 둘지, **동작 동일 + 모바일 레이아웃 최적화**로 둘지.

### 선택지

1. 웹 UI를 그대로 RN에서 따라가기
2. 웹 상호작용 규격을 기준으로 모바일 레이아웃만 최적화
3. 웹뷰 기반 하이브리드 방식

### 추천

- 2번 추천: 웹 동작 규격(문구/버튼/상태 전이)은 유지하고 모바일 UX는 터치 기반으로 조정.

### 사용자 방향

- 추천 기준으로 진행한다.

### 완료 근거

- `apps/mobile/src/lib/mobile-app-mode.ts` 추가 (`EXPO_PUBLIC_MOBILE_VARIANT` 분기)
- `apps/mobile/app.config.js` 추가 (앱 메타 분리)
- `apps/mobile/src/lib/mobile-preview.ts` 추가 및 `apps/mobile/app/_layout.tsx`에서 적용

## 차수 2 — 공통 계약/도메인 정렬(중복 구현 축소)

### 작업내용

- 카드/세션/회원 데이터 타입과 API 스키마의 공유 범위를 정리.
- 세션/헤더 규칙을 단일 처리로 정리.
  - `guest:<profileId>` 게스트 토큰 규칙
  - `X-Yeon-Chat-Profile-Id` 헤더 규칙
  - 카드서비스 비로그인 접근 조건/라우팅 규칙
- 공통 UI 프레임/간격/텍스트 토큰 정리.

### 논의 필요

- 공통 코드 범위를 어디까지로 잡을지.

### 선택지

1. 타입/헬퍼만 공유하고 화면은 분리
2. 목록/빈상태/버튼/에러/로딩 패턴만 공통화
3. 현재 분기형 구조 유지

### 추천

- 2번: 패턴 공유 범위만 제한해 유지보수 오버헤드 감소.

### 사용자 방향

- 추천 기준으로 진행한다.

### 현재 상태

- [x] API 계약 확정: `packages/api-contract/src/chat-service.ts` (guest 프로필), `packages/api-contract/src/card-decks.ts` (카드서비스 DTO/응답)
- [x] API 클라이언트 규칙 정리: `packages/api-client/src/index.ts` (`guest:` 헤더/세션 처리)
- [x] 카드서비스 세션 분기 모듈: `apps/mobile/src/features/card-service/card-service-session.ts`
- [x] 공통 UI 패턴 후보 5개 선정(1차): `StateBlock`, `ActionButton`, `TopBar`, `TextField`, 상태/에러 문구 템플릿
  - 적용 대상: `apps/mobile/src/components/ui/state-block.tsx`, `action-button.tsx`, `top-bar.tsx`, `text-field.tsx`
  - 남은 과제: 토스트/모달 피드백 방식은 웹 비교 후 차수 3-4에서 정합

## 차수 3 — 카드서비스 모바일 화면 재작업: 웹 정합 기준 반영

### 작업내용

- 목록/상세/플레이 3대 화면 동선 정렬
  - 로딩/빈상태/에러/성공 상태 전이
  - 비로그인 접근 가능성
  - 상세 진입·학습 시작·종료 플로우
- 웹과 문구/버튼/우선 액션 동기화
- iPhone 14 Pro 기준 프리뷰에서 과도한 반응형 방지

### 진행 상태

- [x] 3-1. 상태 흐름(로딩/빈 상태/에러/로그인 분기) 정렬 완료
  - 근거: `apps/mobile/src/features/card-service/card-deck-list-screen.tsx`, `apps/mobile/src/features/card-service/card-deck-detail-screen.tsx`, `apps/mobile/src/features/card-service/card-deck-play-screen.tsx`
- [x] 3-2. 웹 문구/버튼/액션 라벨 정합 표 작성 및 반영
  - 완료된 항목: 목록 게스트/로그인 부제, 상세 빈덱 메시지, 플레이 라벨 일부 정렬
  - 근거: `apps/mobile/src/features/card-service/card-service-copy.ts`
- [x] 3-3. 상세/리뷰 텍스트 템플릿 최종 표준화
  - 완료된 항목: 상세/플레이 화면 문구 상수 정합(메타/모드/액션/리뷰 텍스트)
  - 근거:
    - `apps/mobile/src/features/card-service/card-deck-detail-screen.tsx`
    - `apps/mobile/src/features/card-service/card-deck-play-screen.tsx`
    - `apps/mobile/src/features/card-service/card-service-copy.ts`

### 논의 필요

- 현재 1차 정합 후 미세 문구/배치 조정 범위

### 선택지

1. 최소 수정 유지
2. 1차 리워크(현재 구현 정리)
3. 전면 재작성

### 추천

- 2번: 핵심 동선 우선 정합 후 추가 공통 컴포넌트 분리 진행.

### 사용자 방향

- 추천 기준으로 진행한다.

### 현재 상태

- [x] 상세/리뷰 플로우에서 공통 텍스트/상태 메시지 표준 템플릿 정합(완료)
- [x] 카드 목록/상세/플레이는 게스트/로그인 분기 동작 반영 상태
  - 근거: `apps/mobile/src/features/card-service/card-deck-list-screen.tsx`,
    `apps/mobile/src/features/card-service/card-deck-detail-screen.tsx`,
    `apps/mobile/src/features/card-service/card-deck-play-screen.tsx`
- [x] 웹 문구/버튼/액션 레이블 동기화 산출물 작성 완료
  - `apps/mobile/src/features/card-service/*` vs `apps/web/src/app/card-service/*` 비교 후 미스 매핑 처리
  - 상세/플레이 화면을 중심으로 문구를 상수 레이어로 정합
- [x] 3-4. 문구/버튼/액션 라벨 터치·접근성 QA
  - 대상: 라벨 가독성, 줄바꿈/개행, 버튼/탭 타깃 크기, 접근성 라벨 누락 항목
  - 완료 근거: 핵심 라벨·액션 항목 텍스트 상수화, 접근성 계약 테스트, 문구 정합 리뷰
- [x] web 모드 화면에서 iPhone 14 Pro 프레임 상향 완료
  - 근거: `apps/mobile/src/lib/mobile-preview.ts`, `apps/mobile/app/_layout.tsx`

## 차수 4 — 익명친구 앱 연동 정합 및 동시 발행 준비

### 작업내용

- 익명친구 OTP 스킵 정책 유지 및 카드서비스와 충돌 없는 분기점 점검
- `pnpm dev:all`에서 카드/익명친구 모바일 동시 기동 보장
- 앱 메타/환경변수 분리 정합 (번들 ID, 포트, 서버 URL, 토큰)

### 논의 필요

- 스토어 노출명/번들 ID/아이콘 분리 정책

### 선택지

1. 단일 앱명/번들로 운영
2. 앱별 분리 운영

### 추천

- 운영 관점은 2번(분리 운영) 유지.

### 사용자 방향

- 추천 기준으로 진행한다.

### 현재 상태

- [x] 동시 기동 스크립트 분리: `scripts/dev-all.mjs` (`mobile-anonymous`, `mobile-card`)
- [x] 인증 스킵 플래그: `apps/mobile/.env.example`, `apps/mobile/README.md`
- [x] 앱 모드/메타 분기: `apps/mobile/app.config.js`, `apps/mobile/src/lib/mobile-app-mode.ts`

## 차수 5 — 단계별 점검/배포 체크

### 작업내용

- 각 차수 종료 시 정적/수동 점검 실행
  - lint/typecheck
  - 데스크톱 iPhone 14 Pro 프리뷰 화면 검증
  - 카드서비스 비로그인 진입/공유 동작 검증
  - 익명친구 인증 스킵 정상 동작 검증
  - `pnpm dev:all` 2개 모바일 프로필 동시 가동 확인

### 논의 필요

- 내부 QA/베타 확장 범위

### 선택지

1. 내부 셀프QA만 진행
2. 베타 확장 병행

### 추천

- 1번(내부 셀프QA)로 시작 후 핵심 항목 통과 시 외부 확장

### 사용자 방향

- 추천 기준으로 진행한다.

### 시작 조건

- 차수 1~4의 실무점검표 항목이 체크 기준 충족해야 함.
