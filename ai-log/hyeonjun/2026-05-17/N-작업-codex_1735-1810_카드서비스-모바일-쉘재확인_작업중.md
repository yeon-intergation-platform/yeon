# 작업 로그 (2026-05-17)

## 주제

카드서비스 홈 모바일(393px)에서 내 프로필/오늘의 시작 CTA 오른쪽 잘림 재확인

## 차수 1

- 작업내용: `card-service-home.tsx`에서 홈 섹션의 `overflow`를 모바일에서 `overflow-x-visible`로 보정해
  오른쪽 텍스트/요소가 1~2px 단위로 잘리는 현상 제거 시도
- 논의 필요: 없음 (이미 모바일 패딩/카드 고정폭 조정은 운영된 상태)
- 선택지: (1) 기존 `overflow-hidden` 유지, (2) x축 clip 방지 위해 `overflow-x-visible`로 전환
- 추천: `overflow-x-visible`
- 사용자 방향: 추천

## 진행 내용

- `apps/web/src/features/card-service/card-service-home.tsx`
  - `section`에 `overflow-hidden` -> `overflow-x-visible` 변경
