# 작업 로그 (2026-05-17)

## 주제

카드서비스/타이핑 홈 + feature 공용 레이아웃 하드코딩 정리

## 차수 1

- 작업내용: `apps/web/src/features/typing-service/typing-service-home.tsx`의 하드코딩 클래스 문자열을
  `apps/web/src/features/typing-service/typing-service-home.const.ts` 상수로 이전
- 논의 필요: 없음
- 선택지: (1) 기존 카드서비스처럼 분리, (2) 기존 값 유지
- 추천: (1) 카드서비스와 동일 패턴으로 상수 모듈 분리
- 사용자 방향: 추천
- 결과: 완료

## 차수 2

- 작업내용: 공통 하드코딩 후보를 `apps/web/src/features/shared-style-constants.ts`로 상수화
  - `min-h-screen bg-white text-[#111]` → `SHARED_FEATURE_CLASS.pageSurface`
  - `fixed inset-0 z-50 ... bg-black/40 px-4` → `SHARED_FEATURE_CLASS.modalOverlay`
  - `w-full max-w-[420px] ... p-6` → `SHARED_FEATURE_CLASS.modalCard`
  - `inline-flex ... hover:border-[#aaa]` → `SHARED_FEATURE_CLASS.smallInlineActionButton`
- 논의 필요: 없음
- 선택지: (1) 각 파일에서 유지, (2) 공통 상수화
- 추천: (2) 공통 상수화
- 사용자 방향: 추천
- 결과: 완료

## 차수 3

- 작업내용: 타당한 범위에서 화면 루트 컨테이너 공통 상수화 적용 확장
  - 카드룸/타이핑룸/덱 화면/공통 페이지(`profile`, `typing-service/decks/[deckId]`)에서
    루트 `min-h-screen` 하드코딩 문자열 치환
- 논의 필요: 없음
- 선택지: (1) 즉시 치환, (2) 화면별 추가 리팩토링 대기
- 추천: (1) 즉시 치환
- 사용자 방향: 추천
- 결과: 완료

## 검증

- 명령: `pnpm --filter @yeon/web lint`
- 결과: 성공
- 명령: `pnpm --filter @yeon/web typecheck`
- 결과: 성공

## 결과 요약

- 우선순위 1: 완료
- 우선순위 2: 1차 후보(루트/모달/작은 인라인 액션 버튼) 완료
- 미완 후보: `rounded-lg border border-[#e5e5e5] bg-[#fafafa] px-3 py-2`는 동일 패턴이 `typing-room-settings-panel.tsx` 내에서 다수 존재해 추가 추적 대상

## 차수 4

- 작업내용: 타이핑 룸 설정 패널에 남아있던 반복 하드코딩 추적 및 상수화
  - `typing-room-settings-panel.tsx`에서 반복되는 세 패턴을 상수화
    - `SETTINGS_FIELD_CLASS` (`grid gap-1 text-[12px] font-semibold text-[#666]`)
    - `SETTINGS_SELECT_CLASS` (`h-9 rounded-lg border border-[#d7d7d7] px-2`)
    - `SHARED_FEATURE_CLASS.mutedInputPanel` 사용 (`rounded-lg border border-[#e5e5e5] bg-[#fafafa] px-3 py-2`)
- 논의 필요: 없음
- 선택지: (1) 파일 내 하드코딩 유지, (2) 상수화로 유지보수성 개선
- 추천: (2) 상수화
- 사용자 방향: 추천
- 결과: 완료
