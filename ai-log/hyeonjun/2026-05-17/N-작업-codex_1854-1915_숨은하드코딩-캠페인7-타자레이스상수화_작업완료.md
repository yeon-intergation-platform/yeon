# 숨은 하드코딩 캠페인 7 - 타자 레이스 상수화

## 작업내용

- 타자 레이스 solo/multiplayer/practice 화면의 반복 레이아웃/상태 색상 className을 `TYPING_SERVICE_COMMON_CLASS`로 승격했다.
- 엔진 뷰포트, 레이스 스탯 행, 스탯 라벨/구분자, 결과 값, 프롬프트 문자 상태(mismatch/matched/current/pending)를 공통 상수로 정리했다.
- multiplayer 화면에 남아 있던 prompt panel/input area 직접 문자열을 기존 레이스 공통 상수로 교체했다.

## 논의 필요

- 없음. UI 동작 변경 없이 하드코딩 문자열 중복만 축소했다.

## 선택지

1. 레이스 화면 내부 문자열 상수화만 진행한다.
2. 레이스 스탯 행을 별도 컴포넌트로 추출한다.
3. 전체 타이핑 화면을 한 번에 대규모 정리한다.

## 추천

- 1번. 기존 누적 변경이 큰 상태라 이번 라운드는 안전한 className 상수화로 한정한다.

## 사용자 방향

- 다음 진행.

## 검증

- `git diff --check -- apps/web/src/features/typing-service/typing-race-solo-screen.tsx apps/web/src/features/typing-service/typing-race-multiplayer-screen.tsx apps/web/src/features/typing-service/typing-race-solo-practice-panel.tsx apps/web/src/features/typing-service/typing-service-common.const.ts`
- 타자 레이스 3개 파일 간 직접 동일 `className="..."` 중복 검사: 출력 없음.
