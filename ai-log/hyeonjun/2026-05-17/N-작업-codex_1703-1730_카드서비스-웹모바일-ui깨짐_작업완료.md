# 작업 로그 (2026-05-17)

## 주제

카드 서비스 웹 모바일 화면 critical UI 깨짐 수정

## 차수 1

- 작업내용:
  - 운영 `https://yeon.world/card-service`를 iPhone 14 Pro 폭으로 재현한다.
  - 홈 화면에서 오른쪽으로 잘리는 고정 폭 UI를 찾아 수정한다.
  - 모바일 폭 overflow, lint/typecheck로 검증한다.
- 논의 필요: 없음
- 선택지:
  1. 홈 화면만 폭 제한 수정
  2. 공용 프로필 카드까지 반응형 수정
- 추천: 2번
- 사용자 방향: 추천 기준

## 진행

- [x] 운영 393px 재현: `TypingProfileCard` 및 오늘의 시작 영역이 421px까지 밀려 horizontal overflow 발생
- [ ] 코드 수정
- [ ] 검증
- [x] 코드 수정
  - `TypingProfileCard`의 `w-[380px]` 고정 폭을 `w-full max-w-[380px] min-w-0`로 변경
  - 카드 서비스 홈 좌/우 패널에 `min-w-0` 적용 및 모바일 패딩 축소
  - 오늘의 시작 CTA에 `w-full` 적용
- [x] 검증
  - `pnpm --filter @yeon/web lint` 통과
  - `pnpm --filter @yeon/web typecheck` 통과
  - `git diff --check` 통과

## 2차 hotfix (운영 이전 번들 잔존 확인 후 보강)

- 운영 DOM에서 아직 `w-[380px]`, `p-5` 이전 클래스가 확인되어 모바일 overflow가 계속 재현됨.
- 후속 배포가 확실히 들어가도록 모바일 shell 여백을 추가 축소하고, 프로필 카드는 기본 `max-w-full` + `sm:max-w-[380px]`로 더 보수적으로 변경.
