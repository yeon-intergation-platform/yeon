# 작업 로그 (2026-05-17)

## 주제

숨은 하드코딩 정리 2차 캠페인: 라운드1.5 추가 반영(타이핑 핵심군)

## 차수

- 작업내용:
  - `shared-style-constants.ts`에 `inlineItemsCenterGap2` 추가
  - `typing-service-common.const.ts`에 레이스/결과값 공통 클래스(`metricValue`, `metricResultValue`, `raceStatRowBase`, `raceStatValueRow`, `raceInputArea`, `racePromptTextPanel`, `raceResultCard`) 추가
  - 아래 파일의 하드코딩 패턴을 상수로 치환
    - `typing-race-solo-practice-panel.tsx`
    - `typing-race-solo-screen.tsx`
    - `typing-race-multiplayer-screen.tsx`
    - `character-frame-admin.tsx`
    - `typing-bgm-button.tsx`
- 작업 결과:
  - 후보 `text-[20px] font-bold` 중복군(6건) 및 `text-[16px] text-[#111]` 중복군(6건) 제거
  - `flex items-center gap-2` 중복 일부(2건) 상수로 묶음
  - `mt-3 flex items-center gap-6 ...`/`rounded-lg border...px-6 py-5...`/입력 textarea 3중복군도 제거

## 검증

- `python3` 후보 탐지 스크립트(typing-service 기준 2회 이상 반복):
  - 삭제 이전 대비 `text-[20px] font-bold` 및 `text-[16px] text-[#111]` 빈도 0건 처리
  - 현재 상위 2회 반복군 9개 미만으로 감소(기준은 추가 반영 대상군 기준)
- `pnpm --filter @yeon/web lint`: 실행 완료(통과)
- `pnpm --filter @yeon/web typecheck`: 실행 완료(실패, 기존 타입 에러 3건)
  - `TYPING_SERVICE_COMMON_CLASS.sectionBodyGap3` 누락으로 발생
  - `apps/web/src/features/typing-service/typing-service-common.const.ts`에 `sectionBodyGap3` 추가 후 재실행
- `pnpm --filter @yeon/web typecheck`: 재실행 완료(통과)

## 차수 추가 (2026-05-17 20:35~)

- 작업내용:
  - 타입체크 회귀로 잡힌 `sectionBodyGap3` 상수 부재를 복구
- 결과:
  - `apps/web/src/features/typing-service/typing-service-common.const.ts`:
    - `sectionBodyGap3: "mt-3 grid gap-3"` 추가
  - `pnpm --filter @yeon/web typecheck` 재실행 성공
