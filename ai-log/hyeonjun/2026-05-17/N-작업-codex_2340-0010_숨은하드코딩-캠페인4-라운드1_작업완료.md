# 작업 로그 (2026-05-17)

## 차수 1

- 작업내용
  - 숨은 하드코딩 캠페인 2차 라운드로 `text-[12px]/text-[13px] font-semibold` 빈도 조합 중 상위 후보를 재선별 후, 공통 상수 사용으로 교체 시작.
  - 탐지 결과 기준 상위 후보(회귀 기준: 2회 이상):
    - `text-[12px] font-semibold text-[#555]`
    - `text-[13px] font-semibold text-[#666]`
  - 신규 상수 추가: `apps/web/src/features/shared-style-constants.ts`
    - `text12EmphasisMuted`
    - `text13EmphasisMuted`
  - 대상 파일 반영:
    - `apps/web/src/features/typing-service/typing-bgm-button.tsx`
    - `apps/web/src/features/typing-service/typing-settings-button.tsx`
    - `apps/web/src/features/typing-service/typing-room-state-views.tsx`
    - `apps/web/src/features/typing-service/typing-room-waiting-header.tsx`
  - 변경 효과:
    - 대상 파일 내 중복 하드코딩 클래스 4건 `text-[12px] font-semibold text-[#555]` / `text-[13px] font-semibold text-[#666]` 제거
    - shared 상수 적용으로 동일 조합 재사용 진입점 확보
- 논의 필요
  - `text-[12px] font-semibold text-[#777]`, `text-[12px] font-semibold text-[#666]` 같은 5회 후보를 동일 라운드에서 추가할지 여부
- 선택지
  - A안(추천): 상위 2개 조합부터 캠페인 지속 적용
  - B안: 의미군(버튼/레이블/메시지) 우선으로 묶어 한 번에 큰 묶음 반영
- 사용자 방향
  - A안으로 진행

## 검증

- `pnpm --filter @yeon/web lint`
- `pnpm --filter @yeon/web typecheck`

## 이번 라운드 추출 후보(현재 기준 상위 20, 발생 기준 내림차순)

- `text-[15px] font-bold text-[#fffaf4]` (4회)
- `text-[12px] font-semibold text-[#777]` (5회)
- `text-[12px] font-semibold text-[#666]` (5회)
- `text-[12px] font-semibold text-[#555]` (1회※상수 존재)
- `text-[13px] font-semibold text-[#111]` (1회※상수 존재)
- `text-[13px] font-bold text-[#111]` (3회)
- `text-[13px] font-semibold text-[#777]` (3회)
- `text-[14px] font-semibold text-[#111]` (3회)
- `text-[13px] font-bold text-[#666]` (3회)
- `text-[13px] font-bold text-[#111827]` (3회)

## 1차 라운드 반영 후 실측(현재)

- `text-[15px] font-bold text-[#fffaf4]`: 0건 (`text15EmphasisOnCream` 상수 추가 적용 완료)
- `text-[13px] font-semibold text-[#111]`: 0건 (코드 기준 제거)
- `text-[12px] font-semibold text-[#555]`: 0건 (`text12EmphasisMuted` 적용 완료)
- `text-[12px] font-semibold text-[#777]`: 0건 (`text12EmphasisSubtle` 적용 완료)
- `text-[12px] font-semibold text-[#666]`: 5건 (`text13EmphasisMuted`는 #666 13px군, 12px은 12px군 일부 미반영)

## 차수 2

- 작업내용
  - `landing-home`과 `card-row-views`에서 남아있던 `text-[13px] font-semibold text-[#111]` 2건을 상수(`text13Emphasis`)로 정리
  - `community-post-detail-page`와 `community-guest-identity-confirm-modal`의 `text-[12px] font-semibold text-[#555]` 6건 중 실사용 5건을 `text12EmphasisMuted`로 정리
  - `auth-credentials/*` 4개 폼의 `text-[15px] font-bold text-[#fffaf4]` CTA 버튼을 `text15EmphasisOnCream`로 정리
- 논의 필요
  - `text-[12px] font-semibold text-[#777]`(3건)과 `text-[13px] font-semibold text-[#666]`(1건) 이어서 처리할지
- 선택지
  - A안: 크리티컬/반복도 순으로 `text-[12px] font-semibold text-[#777]`부터 진행
  - B안: auth/커뮤니티와 card/typing으로 분기해 의미군(버튼/라벨/상태값) 기준 동시 진행
- 사용자 방향
  - A안(`text-[12px] font-semibold text-[#777]`부터 진행) 추천

## 검증

- `pnpm --filter @yeon/web lint` ✅
- `pnpm --filter @yeon/web typecheck` ✅

## 차수 3

- 작업내용
  - `card-row-views.tsx`와 `card-room-study-panel.tsx`에서 `text-[12px] font-semibold text-[#777]` 2건을 `text12EmphasisSubtle`로 정리
- 검증
  - `pnpm --filter @yeon/web lint` ✅
  - `pnpm --filter @yeon/web typecheck` ✅

## 3차 라운드 추출 후보(현재 기준 상위)

- `text-[13px] font-semibold text-[#777]`: 3건 (다음 우선순위)
- `text-[12px] font-semibold text-[#666]`: 5건

## 차수 4 (현재)

- 작업내용
  - `apps/web/src/features/card-service/card-service-common.const.ts`의 `sectionBadge` 하드코딩 뱃지 클래스를 `SHARED_FEATURE_CLASS.text12EmphasisNeutral` 조합으로 교체
  - `apps/web/src/features/typing-service/typing-deck-library-screen.tsx`의 덱 메타 라벨 클래스 1건을 상수로 교체
  - `apps/web/src/features/typing-service/typing-room-waiting-header.tsx`의 상태/참여자 라벨 `text-[12px] font-semibold text-[#666]` 2건을 상수로 교체
- 검증
  - `pnpm --filter @yeon/web lint` ✅
  - `pnpm --filter @yeon/web typecheck` ✅
- 라운드 결과(현재)
  - `text-[12px] font-semibold text-[#666]`: 0건(직접 문자열)
  - `text-[13px] font-semibold text-[#777]`: 0건(직접 문자열)
