# 작업 로그 (2026-05-17)

## 주제

카드/타이핑 로비 화면에서 남은 공통 하드코딩 클래스 후보 정리 2라운드

## 차수 1

- 작업내용: `apps/web/src/features/room-shared/room-lobby-screen.const.ts` 신설
  - 로비 페이지 공통 구조/타이포/패널 배치 클래스를 의미 기반 상수로 분리
  - `ROOM_LOBBY_CLASS` 항목: `heroSection`, `heroTitle`, `heroDescription`, `listTopBorder`, `filterRow`, `filterScroller`, `inputButtonRow`, `searchField`, `searchIcon`, `roomListRow`, `roomMetaRow`, `roomStatusArea`
- 논의 필요: 없음
- 선택지: (1) 파일별 중복 문자열 유지, (2) room-shared 공통 상수 계층화
- 추천: (2) 로비 레이아웃은 두 서비스가 동일 패턴이라 공통 계층으로 묶는 편이 유지보수성 높음
- 사용자 방향: 추천
- 결과: 완료

## 차수 2

- 작업내용: 공통 상수 사용 적용
  - `apps/web/src/features/card-service/card-room-lobby-screen.tsx`
  - `apps/web/src/features/typing-service/typing-room-lobby-screen.tsx`
  - 위 2개 파일의 중복 className(타이틀/설명/필터 영역/검색 패널/리스트 레이아웃 등)에서 12개 후보를 상수로 교체
- 논의 필요: 없음
- 선택지: (1) 점진 개별 치환, (2) 로비 단위 일괄 배치 반영
- 추천: (2)
- 사용자 방향: 추천
- 결과: 완료

## 차수 3

- 작업내용: `ROOM_LOBBY_CLASS`를 `apps/web/src/features/room-shared/index.ts`로 export
- 논의 필요: 없음
- 결과: 완료

## 검증

- 명령: `pnpm --filter @yeon/web lint`
- 결과: 성공
- 명령: `pnpm --filter @yeon/web typecheck`
- 결과: 성공

## 잔여 리스크

- 라운지 간 동일 레이아웃 문자열은 동일 파일 내에서 className 텍스트로 남지 않게 정리되었으나,
  여전히 다른 화면/컴포넌트에서 의미가 겹치는 단일사용 하드코딩이 다수 존재함.
- 100% 제거는 별도 캠페인(동적/1회성 제외 규칙 확정)으로 계속 진행 필요.
