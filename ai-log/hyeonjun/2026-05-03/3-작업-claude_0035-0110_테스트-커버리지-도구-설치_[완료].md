# 테스트 커버리지 도구 설치 + 첫 측정

## 목적

`apps/web` 워크스페이스에 vitest v8 코드 커버리지를 영구 활성화하고, 활성화 직후 첫 측정 결과(콘솔 수치)를 사용자에게 보고. 신규 패키지 설치 없이 이미 설치된 `@vitest/coverage-v8@4.1.4`를 활용.

## 한 일

1. **deep-interview 3 라운드** — 모호도 70.5% → 53.25% → 24% → 10.25% (임계값 20% 통과). 결정:
   - 범위: `apps/web` 단일 워크스페이스 (모노레포 전체 / CI 게이트 / 외부 SaaS는 NON-GOAL)
   - 산출물: 콘솔 text reporter 단일
   - 영속성: vitest.config + package.json 영구 추가
2. **변경 2파일 (이미 #195로 main 머지됨)**:
   - `apps/web/vitest.config.ts` — `coverage: { provider: "v8", reporter: ["text"], exclude: [...] }` 블록 추가
   - `apps/web/package.json` — `"test:coverage": "vitest run --coverage"` 스크립트 추가
3. **검증**: lint/typecheck 통과, 회귀 무관 도메인 부분 측정 표 정상 출력 (Stmts 95.16%, Branches 85.18%, Funcs 97.29%, Lines 96.29%)

## 산출물

- `apps/web/vitest.config.ts` (M, #195 머지)
- `apps/web/package.json` (M, #195 머지)
- `.omc/specs/deep-interview-test-coverage.md` (로컬, .gitignore — spec 영속화 안 함)

## 발견 사항

- **사전 회귀 82개** 발견 (`pnpm --filter @yeon/web test:coverage` 풀 실행 시). 우리 변경과 무관, 본 세션 시작 시점부터 main에 존재. 회귀 묶음 4개:
  - typing-decks 데이터 fixture 미일치 (`진달래꽃 (시집)` ↔ `진달래꽃`, `손자병법 / The Art of War` ↔ `The Art of War`)
  - `cloud-oauth-service` callback URI: `/api/v1/...` ↔ `/counseling-service/api/v1/...` (counseling 라우트 분리 회귀)
  - `counseling-records-service`/`member-fields-service` schema mock에서 `counselingRecords`/`spaces` export 누락
  - `import-drafts-service`/`analyze-route` mock 호출 누락 / draft state 정리 누락
- 풀 실행 시 회귀로 인한 worker 종료 경로에서 v8 reporter flush 실패 → coverage 표 출력 누락. **부분 측정**에서는 정상.
- **다른 worktree 9개 active** (`.omx/team/implement-the-approved-plan-in/worker-1~5`, `yeon-typing-rewards-team` 가족 4개). 회귀 정리는 #194 (22건) → #195 (22→26 fast-follow)로 다른 워커가 분할 진행 중인 신호. 본 세션에서는 회귀 fix에 진입하지 않음 (충돌 위험).

## 다음 단계 (다른 워커/세션)

- 회귀 4개 묶음 fast-follow 정리 — 차수별 PR로 진행 중인 흐름 유지
- 회귀 전수 정리 후 `pnpm --filter @yeon/web test:coverage` 재실행 시 콘솔 표 전체 출력 가능 (구조는 이미 main에 들어가 있음)

## 범위

- 변경 파일 2개 (`apps/web/vitest.config.ts`, `apps/web/package.json`) — 이미 #195로 main 머지됨
- 회귀 fix 0건 (의도적 비포함)
- ralph cancel로 종료
