# 카드방 런타임 연결 복구 작업 로그

- 시작: 2026-05-18 00:17 KST
- 워크트리: `/home/osuma/coding_stuffs/yeon-3`
- 브랜치: `fix/card-room-runtime-bridge`
- 증상: 카드방 상세에서 `Failed to fetch`, `Spring 카드방 상태를 불러오는 중입니다.` 문구 노출.
- 초기 원인 가설: `scripts/dev-all.mjs`가 race-server에 backend URL을 넘기지 않고, web에 실제 race-server 포트를 넘기지 않아 카드방 상태 브리지가 깨질 수 있음.

## 진행 메모

- `scripts/dev-all.mjs` 확인 결과 web에는 실제 race-server 포트가 전달되지 않고, race-server에는 backend URL/token/profile이 전달되지 않았다.
- 이 상태에서는 `pnpm dev:all`이 backend를 8080 등 가변 포트로 띄워도 card-room race-server는 기본값 8081로 카드방 상태를 조회할 수 있다.
- 사용자 화면의 `Spring` 문구와 원문 `Failed to fetch` 노출을 제거한다.

## 수정 내용

- `scripts/dev-all.mjs`: web에 `NEXT_PUBLIC_RACE_SERVER_URL`을 실제 race-server 포트로 주입하고, race-server에 backend URL/internal token/profile을 주입.
- 카드방 UI: 사용자 화면에서 내부 프레임워크명 제거.
- 카드방 연결 오류: 브라우저/네트워크 원문 `Failed to fetch`를 사용자용 안내로 정규화.

## 검증

- `node --check scripts/dev-all.mjs`
- `pnpm --filter @yeon/web lint`
- `pnpm --filter @yeon/web typecheck`
- `pnpm --filter @yeon/race-server typecheck`
- `git diff --check`
- `pnpm --filter @yeon/web build`
