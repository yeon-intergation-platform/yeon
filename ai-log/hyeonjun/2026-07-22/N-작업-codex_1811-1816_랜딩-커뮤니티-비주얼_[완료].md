# 랜딩 커뮤니티 카드 전용 비주얼 적용

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 18:11 KST
- 종료: 2026-07-22 18:16 KST
- 대상: `/` 랜딩 4번 커뮤니티 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-15.md`

## 작업 방향

- 제공된 1536×1024 이미지는 채팅 말풍선과 공지 패널까지 합쳐진 완성형 비주얼로 사용한다.
- 커뮤니티 카드에만 전용 이미지 경로와 크롭 위치를 선언한다.
- 1~3번 전경, 5~8번 공통 배경, 카드 헤더·설명·CTA 구조는 유지한다.
- 기존 localhost:3000 개발 서버를 재사용한다.
- Playwright로 변경 전후 375px·1040px·1440px 화면을 남긴다.

## 디자인 점검

- `design-workflow`: 기존 카드 계층과 제공 이미지의 적용 범위를 먼저 고정했다.
- `ui-ux-pro-max`: 보라색 커뮤니티 톤, 반응형 375px·1024px·1440px 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 Playwright 실화면 기준으로 직접 보정한다.
- `design-eye`: 인물, 채팅 말풍선, 공지 패널의 시각 중심과 제목·상태·CTA 가독성을 확인한다.

## 작업 결과

- 원본 PNG를 해상도 변경 없이 1536×1024, 172KB WebP로 최적화했다.
- `FrameBreakArtwork`에 선택적 배경 경로·대체 텍스트·클래스를 추가하고 커뮤니티 항목에만 전용 비주얼을 선언했다.
- 다른 7개 카드의 공통 백지학습 배경과 1~3번의 전경 설정은 유지했다.
- 별도 CSS 그림이나 생성 요소 없이 제공 이미지 한 장을 그대로 사용했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-community-visual-screenshots/`
- 375px: 원본 비율에 가까운 화면, 인물·좌우 말풍선·공지 패널 노출, 가로 스크롤 없음.
- 1040px: 2열 카드에서 원본 전체 구도와 설명·CTA 분리 유지, 가로 스크롤 없음.
- 1440px: 3열 카드에서 인물·주요 말풍선·공지 패널 노출, 상하단만 제한적으로 크롭, 가로 스크롤 없음.
- localhost HTTP 200, 공통 배경 7개·커뮤니티 비주얼 1개·기존 전경 3개를 확인했다.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm verify:parity` 통과 — 검증 개념 14종, registry 개념 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
- `pnpm --filter @yeon/web build` 통과 — Spring 미기동 시 내장 콘텐츠 fallback 경고만 발생
