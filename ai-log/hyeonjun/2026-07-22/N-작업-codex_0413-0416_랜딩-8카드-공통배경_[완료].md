# 랜딩 8개 서비스 카드 공통 배경

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 04:13 KST
- 종료: 2026-07-22 04:16 KST
- 대상: `/` 랜딩 1~8번 서비스 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-13(완).md`

## 작업 방향

- 2번 백지학습의 배경을 8개 카드 공통 SSOT로 사용한다.
- 1~3번 전경은 그대로 유지하고 배경만 통일한다.
- 4~8번은 선택적 전경 구조로 전환해 같은 배경 이미지 영역을 표시한다.
- 제목·상태·설명·CTA 구조와 대비는 유지한다.
- Playwright로 변경 전후 및 375px·1040px·1440px 화면을 확인한다.

## 작업 결과

- `LANDING_CARD_BACKGROUND_SRC`를 추가해 백지학습 배경 경로를 단일 SSOT로 만들었다.
- 8개 서비스 모두 프레임브레이크 카드 설정과 1~8번 순번을 갖도록 확장했다.
- 전경 설정을 선택값으로 분리해 1~3번은 기존 전경을 유지하고 4~8번은 공통 배경만 표시한다.
- 사용되지 않는 `typing-frame-break-background-v3.webp`, `card-deck-frame-break-background.webp`를 제거했다.

## 디자인 점검

- `ui-ux-pro-max`: 교육 서비스 랜딩 카드의 일관성 및 반응형 기준을 확인한다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 카드 구조와 Playwright 실화면을 기준으로 적용했다.
- `design-eye`: 8개 카드의 위계, 반복 배경, 모바일 레이아웃, CTA 가독성을 점검한다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-shared-background-screenshots/`
- 변경 전: 1~3번만 이미지 카드이며 카드별 배경이 달랐다.
- 변경 후: 1~8번 모두 동일한 백지학습 배경을 사용한다.
- Playwright DOM 검사: 375px·1040px·1440px 각각 공통 배경 8개, 고유 이미지 URL 1개, 가로 스크롤 없음.
- localhost 페이지 HTTP 200 확인.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm verify:parity` 통과 — 검증 개념 14종, registry 개념 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
- `pnpm --filter @yeon/web build` 통과 — Spring 미기동 시 내장 콘텐츠 fallback 경고만 발생
