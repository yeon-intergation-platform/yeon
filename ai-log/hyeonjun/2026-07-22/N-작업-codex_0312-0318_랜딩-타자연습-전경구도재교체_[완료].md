# 랜딩 키보드 타자연습 전경 구도 재교체

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 03:12 KST
- 종료: 2026-07-22 03:18 KST
- 대상: `/` 랜딩 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-8.md`

## 작업 방향

- 새 원본에서 방 배경과 투명 전경을 분리한다.
- 기존 카드 컴포넌트와 CSS는 유지하고 v3 이미지 경로 및 실제 크기만 교체한다.
- localhost에서 이미지 로드, 제목 안전영역, 반응형 가로 스크롤을 확인한다.

## 작업 결과

- `typing-frame-break-background-v3.webp`: 1412×480 방 배경
- `typing-frame-break-foreground-v3.webp`: 1448×580 투명 전경
- 외곽 연결 흰 영역과 손·키보드 사이의 닫힌 흰 영역만 투명화했다.
- 전등 내부 빛을 포함한 전경 요소는 원본 이미지 픽셀 그대로 유지했다.
- 기존 JSX와 Tailwind 클래스는 유지하고 v3 경로 및 높이만 교체했다.
- PR에만 존재하던 중간 v2 에셋은 최종 결과에서 제거했다.

## 디자인 점검

- `ui-ux-pro-max`: 고대비, 모바일 스택, 단일 CTA 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 검증된 카드 구조를 유지했다.
- `design-eye`: 제목 위계, CTA, 375px 비겹침, 불필요한 장식·CSS 보정 부재를 점검했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-typing-artwork-v3-screenshots/`
- 375px: v3 전경 1448×580 로드, 제목 비겹침, 가로 스크롤 부재
- 1040px: v3 전경 로드, 제목 비겹침, 가로 스크롤 부재
- localhost 페이지와 v3 에셋 HTTP 200 확인

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
