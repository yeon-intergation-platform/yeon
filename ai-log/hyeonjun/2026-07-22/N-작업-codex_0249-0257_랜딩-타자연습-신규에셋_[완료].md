# 랜딩 키보드 타자연습 신규 에셋

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 02:49 KST
- 종료: 2026-07-22 02:57 KST
- 대상: `/` 랜딩 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-7.md`

## 작업 결과

- 제공 이미지의 방 영역을 `typing-frame-break-background-v2.webp` 1412×510 배경으로 분리했다.
- `-fuzz 6%` 연결 영역 제거로 외곽 흰 배경을 투명화한 `typing-frame-break-foreground-v2.webp` 1448×552 전경을 만들었다.
- 전등 내부는 회전 타원 알파 마스크로 원본 픽셀을 복원해 흰 조명과 전구를 보존하고 외부 흰 배경은 남기지 않았다.
- 모니터·키보드·책상·머그컵·인물은 하나의 투명 전경 이미지에 포함했다.
- 기존 `FRAME_BREAK_ARTWORK_BY_SERVICE`의 `typing-service` 경로와 실제 크기만 v2로 교체했다.
- CSS로 전등이나 잘린 부분을 그리거나 연장하지 않았다.

## 디자인 점검

- `ui-ux-pro-max`: 고대비, 모바일 스택, 단일 CTA 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 검증된 카드 컴포지션을 기준으로 진행했다.
- `design-eye`: 연결된 SSOT 파일이 저장소에 없어 기존 카드의 제목 안전영역, CTA 위계, 모바일 비겹침 기준으로 직접 점검했다.

## 시각 검증

- before/after: `ai-log/hyeonjun/2026-07-22/landing-typing-artwork-v2-screenshots/`
- 375px: 제목·운영 배지와 전경 비겹침, `scrollWidth === clientWidth` 확인
- 480px: 전경/배경 이미지 로드 및 가로 스크롤 부재 확인
- 1040px: 새 타자연습 이미지와 기존 카드 구조 유지 확인
- Playwright 콘솔 오류는 `127.0.0.1` 개발 HMR 교차 출처 차단만 확인됐으며 서비스 화면과 이미지 로드는 정상이다.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
