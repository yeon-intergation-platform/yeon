# 랜딩 타자연습 제목 전경 간격

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 03:46 KST
- 종료: 2026-07-22 03:48 KST
- 대상: `/` 랜딩 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-11.md`

## 작업 방향

- 프레임 브레이크 설정에 서비스별 전경 하단 위치를 둔다.
- 타자연습 전경만 Tailwind 기본 scale 기준 24px 아래로 이동한다.
- 전등 도형이나 이미지 연장 CSS는 추가하지 않는다.
- localhost에서 모바일·데스크톱 구도를 확인한다.

## 작업 결과

- `FrameBreakArtwork`에 `foregroundBottomClassName`을 추가했다.
- 타자연습은 `-bottom-8`, 백지학습·플래시카드 덱은 기존 `-bottom-2`를 사용한다.
- 타자연습 전경 전체가 기존보다 24px 아래로 이동해 제목과 전등 사이에 여백이 생겼다.
- 전등 이미지와 CSS 도형은 수정·추가하지 않았다.

## 디자인 점검

- `ui-ux-pro-max`: 서비스 카드 이미지·제목 간격과 반응형 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 Playwright 실화면으로 직접 조정했다.
- `design-eye`: 제목 위계, 전등 전체 노출, 다른 카드 배치 유지, CTA 가독성을 점검했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-typing-spacing-screenshots/`
- 375px: 제목과 전등 상단 사이 여백, 전등 전체 노출, 가로 스크롤 부재
- 1040px: 전등 갓·상단 관절 노출, 제목·상태·CTA 가독성 유지
- localhost 페이지 HTTP 200 확인

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
