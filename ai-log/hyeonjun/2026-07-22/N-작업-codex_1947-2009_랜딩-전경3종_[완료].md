# 랜딩 커뮤니티·Today·뉴스 전경 통일

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 19:47 KST
- 종료: 2026-07-22 20:09 KST
- 대상: `/` 랜딩 4번 커뮤니티, 5번 YEON Today, 7번 뉴스 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-17.md`

## 작업 방향

- 제공 이미지에서 흰색·체커보드·하단 합성 배경을 제거해 투명 전경을 만든다.
- 기존 공통 백지학습 배경 파일과 1·2·3번 전경은 수정하지 않는다.
- 커뮤니티 전용 완성형 배경 override를 제거하고 공통 배경 위 전경 구조로 통일한다.
- 신규 9번 카드는 요청 범위 밖이므로 변경하지 않는다.
- 기존 localhost:3000 개발 서버를 재사용하고 Playwright로 375px·1040px·1440px를 검증한다.

## 디자인 점검

- `imagegen`: 원본 구도와 UI 내용을 유지한 채 배경만 분리한다.
- `design-workflow`: 공통 배경과 전경 레이어 책임을 분리한다.
- `ui-ux-pro-max`: 375px·1040px·1440px 반응형, 가독성, 모션 감소 설정을 확인한다.
- `design-eye`: 인물·전등·모니터·말풍선의 외곽과 제목·상태 배지 충돌을 확인한다.

## 이미지 생성·분리

- 모드: built-in `imagegen` 이미지 편집 + `#00FF00` 크로마키 + 공식 `remove_chroma_key.py` 알파 변환.
- 뉴스 프롬프트 방향: 상단의 인물·의자·책상·모니터·전등·신문·소품과 원본 글자를 그대로 보존하고 하단 방 패널 및 흰 배경만 제거한다.
- Today 프롬프트 방향: 인물·의자·책상·모니터 UI·전등·할 일 보드·소품과 원본 글자를 그대로 보존하고 체커보드 픽셀만 제거한다.
- 커뮤니티 프롬프트 방향: 인물·책상·모니터·전등·말풍선·반응 배지·접속자·공지 패널을 그대로 보존하고 흰 배경만 제거한다.
- 생성 원본은 `/Users/osuma/.codex/generated_images/019f83ab-f690-7c31-b5d4-df0520476117/`에 유지했다.

## 작업 결과

- `community-frame-break-foreground-v2.webp` 1482×967, 투명 WebP 전경 추가.
- `today-frame-break-foreground-v1.webp` 1536×1024, 투명 WebP 전경 추가.
- `news-frame-break-foreground-v1.webp` 1446×675, 투명 WebP 전경 추가.
- 커뮤니티 완성형 배경 override를 제거하고 공통 배경 상수만 렌더링하도록 정리했다.
- 공통 배경 파일은 `origin/main`과 같은 Git 객체 해시를 유지했다.
- 상태 배지 레이어를 전경보다 위로 올려 제목 영역 가독성을 보장했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-remaining-foregrounds-screenshots/`
- 375px·1040px·1440px before/after 스크린샷을 남겼다.
- 모든 검증 폭에서 공통 배경 9개, 투명 전경 6개, 기존 커뮤니티 완성형 배경 0개, 가로 overflow 0px를 확인했다.
- localhost:3000 HTTP 200을 확인했으며 Spring 8080과 기존 웹·race-server를 함께 유지했다.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm verify:parity` 통과 — 검증 개념 14종, registry 개념 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
- `pnpm --filter @yeon/web build` 통과 — 공개 콘텐츠 내장 fallback 경고만 발생
