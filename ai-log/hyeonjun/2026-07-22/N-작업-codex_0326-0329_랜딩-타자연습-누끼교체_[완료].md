# 랜딩 키보드 타자연습 누끼 교체

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 03:26 KST
- 종료: 2026-07-22 03:29 KST
- 대상: `/` 랜딩 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-9.md`

## 작업 방향

- 제공 이미지의 외곽 흰 배경만 제거한 v4 투명 전경을 만든다.
- v3 방 배경과 기존 카드 JSX/CSS는 유지한다.
- localhost에서 누끼 품질, 제목 비겹침, 가로 스크롤 부재를 확인한다.

## 작업 결과

- `typing-frame-break-foreground-v4.webp`: 1574×792 투명 전경
- 외곽 연결 흰 영역과 팔·키보드 사이의 닫힌 흰 영역만 제거했다.
- 전등 내부 빛과 전경 요소는 원본 픽셀 그대로 보존했다.
- v3 방 배경 및 기존 카드 JSX/CSS를 유지하고 전경 경로·크기만 바꿨다.
- 사용하지 않게 된 v3 전경은 최종 결과에서 제거했다.

## 디자인 점검

- `ui-ux-pro-max`: 고대비, 모바일 스택, 단일 CTA 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 검증된 카드 구조를 유지했다.
- `design-eye`: 제목·상태 위계, 375px 레이아웃, CSS 보정 부재를 점검했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-typing-artwork-v4-screenshots/`
- 375px: v4 전경 로드, 제목·운영 배지 가독성, 가로 스크롤 부재
- 1040px: v4 전경 로드, 카드 레이아웃과 CTA 유지, 가로 스크롤 부재
- localhost 페이지와 v4 전경 HTTP 200 확인

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
