# 랜딩 타자연습 전등 잘림 보정

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-22 03:34 KST
- 종료: 2026-07-22 03:40 KST
- 대상: `/` 랜딩 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-22-landing-frame-break-service-card-10.md`

## 작업 방향

- v4 전경의 실제 픽셀은 변경하지 않는다.
- 왼쪽 투명 캔버스를 늘려 전등 구도를 카드 안쪽으로 이동한다.
- CSS 전등 생성·연장 없이 localhost 모바일·데스크톱 화면을 확인한다.

## 작업 결과

- `typing-frame-break-foreground-v8.webp`: 1574×792 투명 전경
- v4 전경의 실제 픽셀을 투명 캔버스 안에서 180px 오른쪽으로 이동했다.
- 전등 갓, 상단 관절, 세로 암이 카드 왼쪽 경계 안에 보이도록 했다.
- 카드 JSX/CSS는 유지하고 전경 파일 경로만 변경했다.
- 중간 조정용 v5~v7 파일은 저장소에 남기지 않았다.

## 디자인 점검

- `ui-ux-pro-max`: 모바일 이미지 크롭과 CTA 위계 기준을 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 Playwright 실화면을 기준으로 직접 조정했다.
- `design-eye`: 전등 경계, 제목·상태 가독성, 가로 스크롤 부재를 점검했다.

## 시각 검증

- 경로: `ai-log/hyeonjun/2026-07-22/landing-typing-artwork-v8-screenshots/`
- 375px: 전등 갓·상단 관절·세로 암 노출, 가로 스크롤 부재
- 1040px: 전등 전체 노출, 제목·상태·CTA 가독성 유지
- localhost 페이지와 v8 전경 HTTP 200 확인

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
