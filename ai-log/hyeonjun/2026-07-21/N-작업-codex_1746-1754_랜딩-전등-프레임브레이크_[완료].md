# 랜딩 전등 프레임 브레이크 보완

- 작업자: codex
- 상태: 완료
- 작업 시간: 2026-07-21 17:46~17:54 KST
- 대상: `/` 랜딩의 `typing-service` 카드
- 백로그: `docs/product/backlog/2026-07-21-landing-frame-break-service-card-2.md`

## 작업 내용

- 기존 투명 전경을 이미지 영역과 헤더 돌출 영역에 각각 정렬해 전등과 인물이 모두 경계를 넘도록 했다.
- 헤더 높이를 64px에서 96px로 늘리고 제목 행 아래에 32px 패딩을 확보했다.
- 화면 전체를 가리는 마스크 대신 전경의 자연스러운 외곽을 유지해 머리와 전등이 직선으로 잘리지 않도록 했다.
- 웹 전용 랜딩 레이아웃 변경이므로 모바일 앱 코드는 변경하지 않았다.

## 디자인 근거

- 흰색 미니멀 카드와 기존 CTA·상태 위계를 유지했다.
- 제목·번호·상태는 상단 44px 안에 배치하고, 프레임 브레이크 전경은 그 아래에서 시작하도록 분리했다.
- 21st 리파이너는 API 키 오류로 사용할 수 없어 기존 코드와 실제 스크린샷을 기준으로 조정했다.

## 스크린샷

- 폴더: `ai-log/hyeonjun/2026-07-21/landing-lamp-frame-break-screenshots/`
- `after-landing-services-desktop.png`: `/`, 1440×1000, 전등 갓·인물 머리 돌출과 제목 안전영역 확인
- `after-landing-services-mobile.png`: `/`, 375×812, 모바일 카드 겹침·가로 스크롤 없음 확인
- 변경 전 증거: 이전 작업의 `ai-log/hyeonjun/2026-07-21/landing-frame-break-card-screenshots/after-landing-services-*.png`

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm verify:parity` 통과 — 14개 검사, registry 29개 개념
- `pnpm --filter @yeon/web build` 통과
- Playwright 375px/1440px: HTTP 200, 이미지 3개 로드, 제목 노출, 가로 스크롤 없음
- `git diff --check` 통과

## 참고

- 빌드 중 Spring 공개 콘텐츠를 불러오지 못한 경고가 있었으나 내장 초기 데이터 fallback으로 빌드는 정상 완료됐다.
- 브라우저 확장 프로그램이 `html` 속성을 주입해 개발 모드 hydration 경고가 1회 있었으며 앱 코드 변경과 무관하다.
