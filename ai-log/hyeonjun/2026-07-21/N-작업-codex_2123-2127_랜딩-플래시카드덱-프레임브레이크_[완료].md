# 랜딩 플래시카드 덱 프레임 브레이크

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-21 21:23 KST
- 종료: 2026-07-21 21:27 KST
- 대상: `/` 랜딩 `card-service` 카드
- 백로그: `docs/product/backlog/2026-07-21-landing-frame-break-service-card-6.md`

## 작업 결과

- 제공 이미지의 보라색 방 영역을 `card-deck-frame-break-background.webp` 1412×425 배경으로 분리했다.
- `-fuzz 6%` 연결 영역 제거로 외곽 흰 배경만 투명화한 `card-deck-frame-break-foreground.webp` 1448×632 전경을 만들었다.
- 전등 빛, 실제 플래시카드, 고양이, 화분, 인물은 전경 이미지 픽셀 그대로 보존했다.
- 기존 `FRAME_BREAK_ARTWORK_BY_SERVICE`에 `card-service` 순서 3과 에셋 정보만 추가해 레이아웃 복제를 피했다.
- CSS로 전등이나 잘린 부분을 그리거나 연장하지 않았다.

## 디자인 점검

- `ui-ux-pro-max`: 모바일 우선, 고대비, 단일 CTA, 여백 중심 위계를 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 검증된 카드 컴포지션을 기준으로 진행했다.
- `design-eye`: 연결된 SSOT 파일이 저장소에 없어 기존 카드의 제목 안전영역, CTA 위계, 모바일 비겹침 기준으로 직접 점검했다.

## 시각 검증

- before/after: `ai-log/hyeonjun/2026-07-21/landing-card-deck-frame-break-screenshots/`
- 375px: 제목·운영 배지와 전경 비겹침, `scrollWidth === clientWidth` 확인
- 480px: 전경/배경 이미지 로드 및 가로 스크롤 부재 확인
- 1040px: 타자·백지 학습 카드 유지와 플래시카드 덱 프레임 브레이크 적용 확인
- Playwright 콘솔 오류는 `127.0.0.1` 개발 HMR 교차 출처 차단만 확인됐으며 서비스 화면과 이미지 로드는 정상이다.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
