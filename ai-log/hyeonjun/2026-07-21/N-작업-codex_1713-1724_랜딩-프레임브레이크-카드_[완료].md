# 랜딩 프레임 브레이크 카드 작업 로그

- 목표: 제공 이미지의 배경/전경을 분리해 랜딩 타자연습 카드에서 실제 프레임 브레이크 품질을 빠르게 확인한다.
- 범위: `apps/web` 랜딩의 타자연습 카드와 해당 정적 이미지 에셋만 수정한다.
- 디자인 방향: 흰 카드와 검정 텍스트/CTA를 유지하고 보라색은 일러스트와 운영 상태에만 사용한다.
- 모바일 패리티: 랜딩의 화면 레이아웃은 `screen-layout-chrome`의 `platform-divergent` 범위이며 공유 계약·상태·queryKey는 변경하지 않는다.
- 검증 예정: web lint, web typecheck, Playwright desktop/mobile, `git diff --check`.

## 결과

- 종료: 17:24
- 에셋:
  - `apps/web/public/images/landing/typing-frame-break-background.webp`
  - `apps/web/public/images/landing/typing-frame-break-foreground.webp`
- 구현: 타자연습 카드만 배경/전경을 분리 렌더링하고, 전경 오른쪽 상단을 재사용해 인물 머리가 헤더 경계를 넘도록 구성했다.
- 시각 검증:
  - `landing-frame-break-card-screenshots/after-landing-services-desktop.png` — `/`, 1440×1000
  - `landing-frame-break-card-screenshots/after-landing-services-mobile.png` — `/`, 375×812 full page
  - 기존 화면 before 캡처는 백엔드 미기동으로 500 화면만 생성되어 증빙에서 제외했다.
- 검증:
  - PASS `pnpm --filter @yeon/web lint`
  - PASS `pnpm --filter @yeon/web typecheck`
  - PASS `pnpm verify:parity`
  - PASS `pnpm --filter @yeon/web build`
  - PASS `git diff --check`
  - PASS Playwright smoke: 375px/1440px 모두 HTTP 200, 이미지 로드, 가로 넘침 없음
- 로컬 실행: `http://localhost:3000`에서 web/backend/race-server를 포함한 `pnpm dev:all` 세션을 유지해 사용자가 즉시 확인할 수 있게 했다.
