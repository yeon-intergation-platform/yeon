# 랜딩 백지 학습 프레임 브레이크

- 작업자: codex
- 상태: 완료
- 시작: 2026-07-21 20:45 KST
- 종료: 2026-07-21 20:53 KST
- 대상: `/` 랜딩 `recall-service` 카드
- 백로그: `docs/product/backlog/2026-07-21-landing-frame-break-service-card-5.md`

## 작업 결과

- 제공 이미지의 보라색 방 영역을 `baekji-frame-break-background.webp` 1414×426 배경으로 분리했다.
- `-fuzz 6%` 연결 영역 제거로 외곽 흰 배경만 투명화한 `baekji-frame-break-foreground.webp` 1454×630 전경을 만들었다.
- 전등의 빛, 전구 아이콘, 흰 노트는 전경 이미지 픽셀 그대로 보존했다.
- `FRAME_BREAK_ARTWORK_BY_SERVICE` 설정으로 타자·백지 학습 카드가 같은 프레임 브레이크 레이아웃을 재사용하도록 했다.
- 제목과 상태 배지는 흰 헤더 안전영역에 유지하고 전경은 배경·헤더 경계를 넘도록 배치했다.

## 디자인 도구

- `ui-ux-pro-max`: 흰 배경, 검정 중심, 모바일 우선, 한 개 CTA 위계를 확인했다.
- 21st refiner: API 키 오류로 결과를 받지 못해 기존 검증된 카드 컴포지션을 기준으로 진행했다.

## 시각 검증

- before/after: `ai-log/hyeonjun/2026-07-21/landing-baekji-frame-break-screenshots/`
- 375px: 전등·캐릭터·전구와 제목·상태 배지 비겹침, `scrollWidth === clientWidth` 확인
- 480px: 전경/배경 이미지 로드 및 가로 스크롤 부재 확인
- 1040px: 타자 카드 유지와 백지 학습 프레임 브레이크 적용 확인
- Playwright 콘솔 오류는 `127.0.0.1` 개발 HMR 교차 출처 차단만 확인됐으며 서비스 화면 요청과 이미지 로드는 정상이다.

## 검증

- `pnpm --filter @yeon/web lint` 통과
- `pnpm --filter @yeon/web typecheck` 통과
- `pnpm --filter @yeon/web build` 통과
- `pnpm verify:parity` 통과 — 14종 검증, registry 29종
- `git diff --check` 통과
- `bash bin/sync-skills.sh --check` 통과
- `bash bin/verify-ssot.sh --project-only` 통과
