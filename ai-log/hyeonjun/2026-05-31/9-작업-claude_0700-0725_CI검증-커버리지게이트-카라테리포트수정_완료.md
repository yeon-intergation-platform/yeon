# CI 첫 실행 검증 + 커버리지 게이트 + Karate 리포트 경로 수정 (후속)

- 작업자: claude (Opus 4.8)
- 일자: 2026-05-31
- 브랜치: `chore/coverage-gate-karate-report` (base: `main`)
- 요청: ① backend-tests.yml 첫 CI 실행 확인·수정 ② JaCoCo 커버리지 임계 핵심 패키지 한정 도입

## ① CI 첫 실행 검증 (PR #552 트리거분)

`Backend Tests & Reports` 워크플로 첫 실행 **양쪽 잡 모두 success**:

- `test-and-report` ✅, `karate-flows` ✅ (2 JDK + postgres service + standalone Karate 구조가 첫 실행에서 정상 동작 — 구조 조정 불필요)
- 업로드 artifact: backend-jacoco-report(3.5MB), backend-junit-results(711KB), backend-restdocs-snippets — **확인**.
- 단, **backend-karate-report 누락**: `run-flows.sh` 가 karate 리소스 디렉터리에서 실행돼 리포트가 `src/test/resources/karate/target/karate-reports/` 에 생성되는데 워크플로는 `apps/backend/target/...` 에서 찾음(`if-no-files-found: warn` 이라 잡은 성공). → **업로드 경로 수정**.

## ② 커버리지 게이트 (전체 % 강제 아님)

`jacocoTestCoverageVerification` 룰을 **P0 요구사항 도메인의 service/controller 패키지에만** instruction COVEREDRATIO ≥ 0.30 으로 설정:

- 대상: `card_decks.route.{service,controller}`, `card_decks.assets.{service,controller}`, `card_decks.merge_guest.service`, `community_chat.{service,controller}`
- 현재 커버리지 측정값: 최저 `card_decks.route.service` 36% → 0.30 하한 통과(헤드룸 확보). 나머지 49~100%.
- 의도: "P0 핵심 로직 테스트 누락/회귀 감지". repository(DB 통합 비중 큼)·DTO·전체 % 강제는 제외.
- `./gradlew test jacocoTestReport jacocoTestCoverageVerification` → **BUILD SUCCESSFUL**(게이트 통과). CI `test-and-report` 단계에도 추가.

## 검증

- CI 첫 실행: 양쪽 잡 success, artifact 3종 업로드 확인(karate 경로만 수정 필요했음).
- 로컬 `jacocoTestCoverageVerification` 통과.
- 워크플로 YAML 문법 유효(이미 실제 Actions 에서 실행됨).

## 메모

- karate 리포트 경로 수정은 다음 CI 실행에서 `backend-karate-report` artifact 업로드로 최종 확인.
- 커버리지 하한은 회귀 감지용 완만한 값. 도메인 성숙도에 따라 점진 상향 가능(전체 % 강제는 계속 지양).
