# 작업 로그: 타자방 로비 구현 이어받기

- 날짜: 2026-05-12
- 작성자: codex
- 상태: 완료
- 목적: TYPING_ROOM_LOBBY 화면정의/기능정의 기준으로 기존 진행분을 이어받아 UI/서버/공유 계약 정합성을 닫는다.

## 이어받은 상태

- 문서 저장 위치 확인
  - `docs/projects/typing-race/typing-room-lobby-screen-definition.md`
  - `docs/product/backlog/typing-room-lobby-backlog.md`
- 기존 코드 변경 확인
  - 공유 계약: 방 설정 변경, 채팅 메시지, 스냅샷 messages 확장
  - race-server: 로비 설정 변경/채팅/준비 상태/시작/입장 차단 일부 구현
  - web: PC 3단/모바일 순서/참여자/설정/채팅/하단 액션 UI 일부 구현

## 남은 점검 항목

- 방장 퇴장 정책을 MVP 기준(방 종료)으로 맞춘다.
- 문서/코드 간 정책 불일치와 화면 문구를 정리한다.
- 변경 파일 기준 lint/typecheck/build 가능한 범위를 검증한다.

## 추가 진행

- 방장 퇴장 정책을 MVP 기준 `방 종료`로 서버 동작 정합화
- 로비 덱 변경 시 클라이언트가 race seed를 재해결해 서버에 함께 전달하도록 계약/화면 연동 보강
- 설정 변경 중 실제 변경이 없는 중복 메시지는 준비 상태를 리셋하지 않도록 서버 비교 로직 보강
- 관련 문서의 방장 퇴장 정책 문구를 `방 종료` 기준으로 갱신

## 검증 기록

- `pnpm --filter @yeon/race-server typecheck`: 통과
- `pnpm --filter @yeon/race-shared typecheck`: 통과
- `pnpm --filter @yeon/web exec eslint src/features/typing-service/typing-room-screen.tsx src/features/typing-service/use-race-room.ts src/features/typing-service/typing-room-lobby-screen.tsx`: 통과
- `git diff --check`: 통과
- `bash bin/sync-skills.sh --check`: 통과
- `bash bin/verify-ssot.sh --project-only`: 통과(스크립트가 프로젝트 git 저장소 검사는 건너뜀으로 보고)
- `pnpm --filter @yeon/web typecheck` 및 `pnpm --filter @yeon/web build`: 로컬 main이 origin/main보다 6커밋 뒤라 기존 analytics export/중복 import 선행 오류로 실패. 작업 브랜치 rebase 후 재검증 필요.
- `origin/main` rebase 후 전체 lint를 막던 기존 미사용 import/테스트 request 타입 오류를 최소 수정해 검증 경로를 복구함.
