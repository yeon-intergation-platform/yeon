# 공통 헤더 + 커뮤니티 피드 구현

- 시작: 2026-05-12 06:20 KST
- 담당: codex
- 브랜치: feat/common-header-community-feed
- 상태: 완료

## 요청

- `docs/product/backlog/common-header-community-screen.md` 1~6차 전체 진행.
- 구현 후 검증, PR(main), merge, release 판단까지 완료.

## 진행 로그

- 최신 `origin/main` fast-forward 후 작업 브랜치 생성.
- 기존 백로그 문서 변경을 작업 브랜치에 복원.

- 공통 `CommonProductHeader` 추가 및 홈/타자/플래시카드/커뮤니티 적용.
- 헤더 BGM은 `lucide-react` `VolumeX`/`Volume2`, 설정은 `Settings`, 내정보는 `CircleUserRound` 기준으로 구성.
- `/community`를 작은 실시간 채팅 + 카테고리 필터 + 글쓰기 펼침 + 게시글 카드 + 우측 인기 글/최근 댓글로 재구성.
- `/community/posts/[postId]` 상세 화면 추가.
- 금지 문구 및 `react-icons` 신규 사용 검색 통과.
- 검증 통과: `pnpm --filter @yeon/web typecheck`, `pnpm --filter @yeon/web lint`, `pnpm --filter @yeon/web build`, `git diff --check`, `pnpm release:verify -- v0.1.0`.
- release 판단: 사용자 가시 신규 화면/흐름이므로 `minor`, root `package.json` 0.0.3 -> 0.1.0.

## 결과

- 구현/검증 완료. PR 생성 및 main merge 단계로 이동.
