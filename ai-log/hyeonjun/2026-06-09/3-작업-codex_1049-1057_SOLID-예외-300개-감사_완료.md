# SOLID / 예외 원칙 300개 감사

## 목표

- SOLID 원칙과 예외 처리 원칙 위반/적용 후보 300개를 실제 코드 근거로 리스트업한다.
- 지켜지지 않는 곳은 고치고, 적용하면 좋은 곳은 공식 작업 항목으로 남긴다.

## 범위

- 유지보수 대상: card-service, typing-service, community, shared packages, Spring backend 중 관련/공통 영역.
- counseling-workspace는 프로젝트 동결 정책에 따라 신규 유지보수 범위에서 제외한다.

## 진행

- `yeon-2`에서 `origin/main` 기반 `codex/solid-exception-300-audit` 브랜치 생성.

- 유지보수 범위에서 정적 후보 835개를 수집하고 원칙별 균형으로 300개를 백로그에 작성.
- 백엔드 인증/카드방/타자 seed/OAuth provider 예외 처리 5곳을 구체 예외 포착 + cause 보존으로 수정.

## 검증

- `./gradlew test --tests '*root_auth*' --tests '*card_rooms*' --tests '*typing_decks*' || ./gradlew test` (apps/backend) 성공.
- `git diff --check` 성공.
- 백로그 번호 `001`~`300` 카운트 확인: 300개.

## 결과

- SOLID/예외 원칙 300개 TODO를 공식 백로그에 등록.
- 예외 처리 원칙 E2/E7 관련 4개 항목을 이번 차수에서 완료 처리.
