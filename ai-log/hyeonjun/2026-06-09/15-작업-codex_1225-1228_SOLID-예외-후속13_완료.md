# SOLID/예외 원칙 후속 13차

## 목표

- 300개 백로그 중 모바일 카드 온보딩 게이트 catch 처리 책임 명확화 항목 88~89를 실제 코드 개선으로 줄인다.
- 이메일/소셜 로그인 catch의 unknown 오류 메시지 변환을 공통 helper로 좁히고 원인을 보존한다.

## 범위

- `apps/mobile/src/features/card-service/card-onboarding-gate.tsx`
- 백로그 항목 88~89

## 진행

- `yeon-2`를 최신 `origin/main`으로 재설정하고 `codex/solid-exception-followup-13` 브랜치 생성.
- `getCardLoginErrorMessage`를 추가해 Error/string/기타 unknown 값을 로그인 오류 메시지로 변환했다.
- 이메일 로그인과 소셜 로그인 catch 블록의 반복 분기를 helper 호출로 교체했다.
- 백로그 항목 88~89를 완료 처리했다.

## 검증

- `CI=true pnpm --filter @yeon/mobile lint`
- `CI=true pnpm --filter @yeon/mobile typecheck`
- `git diff --check`
- 백로그 300개 유지 및 항목 88~89 완료, 로그인 catch의 반복 `error instanceof Error` 분기 제거 검증 스크립트

## 결과

- 모바일 카드 온보딩 로그인 오류 처리 책임이 한 곳으로 모였고, 비-Error 오류 값도 원인 정보가 fallback 메시지에 남는다.
