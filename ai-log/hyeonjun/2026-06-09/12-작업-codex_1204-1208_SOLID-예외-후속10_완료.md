# SOLID/예외 원칙 후속 10차

## 목표

- 300개 백로그 중 모바일 카드 상세 catch 처리 책임 명확화 항목 79~83을 실제 코드 개선으로 줄인다.
- catch 블록마다 반복되는 `error instanceof Error` 분기를 공통 helper로 좁히고 fallback 메시지에 원인을 보존한다.

## 범위

- `apps/mobile/src/features/card-service/card-deck-detail-screen.tsx`
- 백로그 항목 79~83

## 진행

- `yeon-4`를 최신 `origin/main`으로 재설정하고 `codex/solid-exception-followup-10` 브랜치 생성.
- `getCardDetailErrorMessage`를 추가해 Error/string/기타 unknown catch 값을 한 곳에서 메시지로 변환했다.
- 생성/일괄 추가/덮어쓰기/수정/삭제 catch 블록의 반복 분기를 helper 호출로 교체했다.
- 백로그 항목 79~83을 완료 처리했다.

## 검증

- `CI=true pnpm --filter @yeon/mobile lint`
- `CI=true pnpm --filter @yeon/mobile typecheck`
- `git diff --check`
- 백로그 300개 유지 및 항목 79~83 완료, 반복 `error instanceof Error` catch 분기 제거 검증 스크립트

## 결과

- 모바일 카드 상세의 catch 처리 책임이 한 곳으로 모였고, 비-Error throw 값도 fallback에 원인을 붙여 숨기지 않는다.
