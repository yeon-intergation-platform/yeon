# SOLID / 예외 원칙 후속 2차

## 목표

- 기존 300개 백로그에서 다음 P1 예외 항목을 실제 수정한다.
- 수정한 항목은 백로그에서 완료로 표시한다.

## 범위

- 유지보수 대상 서비스와 공통 패키지.
- 상담 워크스페이스는 동결 정책상 제외.

## 진행

- `yeon-4`에서 `origin/main` 기반 `codex/solid-exception-followup-2` 브랜치 생성.

- 백로그 4~13번을 실제 수정 대상으로 선별.
- Java JSON 처리 2곳을 `JsonProcessingException`으로 축소.
- mobile/web/race-server 빈 catch 8곳을 원인 로그, 사용자 안내, `cause` 보존 또는 `SyntaxError` 한정 처리로 수정.
- 웹 카드 편집/이미지/프로필/크기, 커뮤니티 게스트/presence 빈 catch 10곳을 추가로 수정하고 백로그 14~23번 완료 표시.

## 검증

- `apps/backend`: `./gradlew test --tests '*typing_character_frames*' || ./gradlew test` 성공.
- `CI=true pnpm --filter @yeon/web lint` 성공.
- `CI=true pnpm --filter @yeon/web typecheck` 성공.
- `CI=true pnpm --filter @yeon/mobile lint` 성공.
- `CI=true pnpm --filter @yeon/mobile typecheck` 성공.
- `CI=true pnpm --filter @yeon/race-server lint` 성공.
- `CI=true pnpm --filter @yeon/race-server typecheck` 성공.
- `git diff --check` 성공.
- 백로그 번호 카운트 300개, 완료 표시 1~23 확인.

## 결과

- 백로그 4~23번까지 추가 완료 처리.
- 총 완료 항목: 23개.
