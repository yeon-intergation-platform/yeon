# SOLID/예외 원칙 후속 11차

## 목표

- 300개 백로그 중 모바일 카드 덱 목록 catch 처리 책임 명확화 항목 84를 실제 코드 개선으로 줄인다.
- 덱 생성 실패와 목록 조회 실패의 unknown 오류 메시지 변환을 공통 helper로 좁히고 원인을 보존한다.

## 범위

- `apps/mobile/src/features/card-service/card-deck-list-screen.tsx`
- 백로그 항목 84

## 진행

- `yeon-2`를 최신 `origin/main`으로 재설정하고 `codex/solid-exception-followup-11` 브랜치 생성.
- `getCardDeckListErrorMessage`를 추가해 Error/string/기타 unknown 값을 메시지로 변환했다.
- 덱 생성 catch와 목록 query error 표시에서 같은 helper를 사용하도록 교체했다.
- 백로그 항목 84를 완료 처리했다.

## 검증

- `CI=true pnpm --filter @yeon/mobile lint`
- `CI=true pnpm --filter @yeon/mobile typecheck`
- `git diff --check`
- 백로그 300개 유지 및 항목 84 완료, 덱 목록 catch의 반복 `error instanceof Error` 분기 제거 검증 스크립트

## 결과

- 모바일 카드 덱 목록의 catch/query 오류 메시지 변환 책임이 한 곳으로 모였고, 비-Error 오류 값도 원인 정보가 fallback 메시지에 남는다.
