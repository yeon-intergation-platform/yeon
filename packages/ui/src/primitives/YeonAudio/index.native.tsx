import { forwardRef } from "react";

export type YeonAudioHandle = null;
export type YeonAudioProps = {
  autoPlay?: boolean;
  playsInline?: boolean;
  src?: string;
};

export const YeonAudio = forwardRef<YeonAudioHandle, YeonAudioProps>(
  function YeonAudio(_props, _ref) {
    return null;
  }
);
