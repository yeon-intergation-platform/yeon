spring:
  application:
    name: yeon-backend
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: yeon_backend
    schemas: yeon_backend

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
